{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome", // Using Chrome debugger for client-side debugging
        "request": "launch", // Launching a new Chrome instance
        "name": "Launch Chrome against Vite (React + TS)", // Configuration name
        "url": "http://localhost:5173", // URL where your Vite dev server is running
        "webRoot": "${workspaceFolder}/src", // Maps source files for accurate breakpoints
        "sourceMaps": true // Enables source maps for TypeScript debugging
      }
    ]
  }
  